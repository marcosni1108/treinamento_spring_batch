CREATE TABLE IF NOT EXISTS TB_CACHORRO(
	ID INTEGER IDENTITY PRIMARY KEY,
	RACA VARCHAR(100) NOT NULL,
	PRECO DECIMAL(10,2)
);

CREATE TABLE IF NOT EXISTS TB_GATO(
	ID INTEGER IDENTITY PRIMARY KEY,
	RACA VARCHAR(100) NOT NULL,
	PRECO DECIMAL(10,2)
);